import { TitleStringEditorPipe } from './title-string-editor.pipe';

describe('StringEditorPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleStringEditorPipe();
    expect(pipe).toBeTruthy();
  });
});
