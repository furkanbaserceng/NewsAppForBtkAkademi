import { DescriptionStringEditorPipe } from './description-string-editor.pipe';

describe('DescriptionStringEditorPipe', () => {
  it('create an instance', () => {
    const pipe = new DescriptionStringEditorPipe();
    expect(pipe).toBeTruthy();
  });
});
